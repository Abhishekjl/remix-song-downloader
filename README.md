# remix-song-downloader
this is a program to download the remix song using a python program


libraries used :
requests library,
,os 
BeautifulSoup,
tqdm for progress bar


installing the libraries:
type in your terminal
-----------------------
pip install tqdm
pip install requests
pip install beautifulsoup
